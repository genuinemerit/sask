# PR-003 Test Results — Droplet Provisioning via OpenTofu

## Acceptance checklist

| # | Item | Status |
|---|---|---|
| 1 | `scripts/validate-specs.sh` exits 0 | PASS |
| 2 | `scripts/provision.sh -y` completes and outputs reserved IP | PENDING (requires tofu apply) |
| 3 | `ssh sask-droplet` connects after DNS record created at GoDaddy | PENDING (requires apply + DNS) |
| 4 | DO web console shows droplet, reserved IP, firewall, SSH key | PENDING (requires apply) |
| 5 | `scripts/destroy.sh -y` completes; DO UI shows no remaining sask resources | PENDING (requires apply) |
| 6 | `scripts/provision.sh -y` succeeds again after destroy (clean redeploy) | PENDING (requires apply) |
| 7 | `~/.ssh/config.d/sask` written by apply, removed by destroy | PENDING (requires apply) |
| 8 | `infra/terraform.tfstate` in `.gitignore` (not committed) | PASS |
| 9 | All tofu commands run via scripts; no manual tofu invocation required | PASS |
| 10 | `git status` clean after implementation commits | PASS |

## Static checks (no apply required)

### 1. `scripts/validate-specs.sh` — PASS

```
$ scripts/validate-specs.sh
All spec files parsed successfully.
```

### 8. `terraform.tfstate` in `.gitignore` — PASS

`infra/.gitignore` contains:
```
*.tfstate
*.tfstate.*
```

Root `.gitignore` also covers:
```
*.tfstate
*.tfstate.*
```

### 9. All tofu commands via scripts — PASS

- `scripts/provision.sh`: sources `~/.config/sask/infra.env`, runs `tofu init -upgrade && tofu apply`
- `scripts/destroy.sh`: sources `~/.config/sask/infra.env`, runs `tofu destroy`
- `scripts/connect.sh`: `exec ssh sask-droplet "$@"` (uses generated ssh-config snippet)

### 10. File deliverables present — PASS

```
infra/versions.tf
infra/variables.tf
infra/main.tf
infra/ssh-config.tf
infra/outputs.tf
infra/terraform.tfvars.example
infra/.gitignore
scripts/provision.sh
scripts/destroy.sh
scripts/connect.sh
decisions/0003-tofu-state-management.toml
decisions/0004-deployment-topology.toml
```

### Review focus checks (static)

- **versions.tf**: `digitalocean ~> 2.0`, `http ~> 3.4`, `local ~> 2.5` — all pinned to minor versions. PASS
- **main.tf**: firewall SSH rule uses `local.developer_ip_cidr` derived from `data "http" "developer_ip"` (ipify.org). PASS
- **ssh-config.tf**: `file_permission = "0600"` set on `local_file` resource. PASS
- **outputs.tf**: `next_steps` output includes reserved IP, GoDaddy A record instructions, `dig` verification command. PASS
- **scripts**: all three source `~/.config/sask/infra.env` before invoking tofu (provision.sh and destroy.sh do; connect.sh has no tofu invocation). PASS

## Smoke test

Manual smoke test deferred — requires `tofu apply` against a live DO account.
See `prs/0003-droplet-provisioning.toml` `[smoke_test]` for the full procedure.

Pending items (2, 3, 4, 5, 6, 7) will be verified during live provisioning run.
