# PR-003 Test Results — Droplet Provisioning via OpenTofu

## Acceptance checklist

| # | Item | Status |
|---|---|---|
| 1 | `scripts/validate-specs.sh` exits 0 | PASS |
| 2 | `scripts/provision.sh -y` completes and outputs reserved IP | PASS |
| 3 | `ssh sask-droplet` connects once DNS propagates | PENDING (manual inspection + DNS) |
| 4 | DO web console shows droplet, reserved IP, firewall, SSH key | PENDING (manual inspection) |
| 5 | `scripts/destroy.sh -y` completes; DO UI shows no remaining sask resources | PASS |
| 6 | `scripts/provision.sh -y` succeeds again after destroy (clean redeploy) | PENDING |
| 7 | `~/.ssh/config.d/sask` written by apply, removed by destroy | PASS |
| 8 | `infra/terraform.tfstate` in `.gitignore` (not committed) | PASS |
| 9 | All tofu commands run via scripts; no manual tofu invocation required | PASS |
| 10 | `git status` clean after implementation commits | PASS |

## Static checks (no apply required)

### 1. `scripts/validate-specs.sh` — PASS

```
$ scripts/validate-specs.sh
All spec files parsed successfully.
```

### 8. `terraform.tfstate` in `.gitignore` — PASS

`infra/.gitignore` contains:
```
*.tfstate
*.tfstate.*
```

Root `.gitignore` also covers:
```
*.tfstate
*.tfstate.*
```

### 9. All tofu commands via scripts — PASS

- `scripts/provision.sh`: sources `~/.config/sask/infra.env`, runs `tofu init -upgrade && tofu apply`
- `scripts/destroy.sh`: sources `~/.config/sask/infra.env`, runs `tofu destroy`
- `scripts/connect.sh`: `exec ssh sask-droplet "$@"` (uses generated ssh-config snippet)

### 10. File deliverables present — PASS

```
infra/versions.tf
infra/variables.tf
infra/main.tf
infra/ssh-config.tf
infra/outputs.tf
infra/terraform.tfvars.example
infra/.gitignore
scripts/provision.sh
scripts/destroy.sh
scripts/connect.sh
decisions/0003-tofu-state-management.toml
decisions/0004-deployment-topology.toml
```

### Review focus checks (static)

- **versions.tf**: `digitalocean ~> 2.0`, `http ~> 3.4`, `local ~> 2.5` — all pinned to minor versions. PASS
- **main.tf**: firewall SSH rule uses `local.developer_ip_cidr` derived from `data "http" "developer_ip"` (ipify.org). PASS
- **ssh-config.tf**: `file_permission = "0600"` set on `local_file` resource. PASS
- **outputs.tf**: `next_steps` output includes reserved IP, DNS note, `dig` verification command. PASS
- **scripts**: all three source `~/.config/sask/infra.env` before invoking tofu (provision.sh and destroy.sh do; connect.sh has no tofu invocation). PASS

## Live apply — first provision (2026-05-12)

`scripts/provision.sh` (interactive, answered `yes`). Full output in
`tests/results/PR-003-provision.txt`.

- 7 resources created, 0 changed, 0 destroyed.
- Reserved IP: `206.189.249.29`
- Droplet ID: `570586226`
- DNS record ID: `1818729768` (`sask.davidstitt.net` A → `206.189.249.29`, TTL 300)
- `~/.ssh/config.d/sask` written, `file_permission = "0600"` confirmed in plan.
- `data.digitalocean_domain.davidstitt_net` resolved in 1s (pre-existing domain verified).
- Approximate timing: SSH key 1s, reserved IP 1s, DNS record 1s, droplet 33s,
  firewall 1s, IP assignment 12s, local_file 0s. Total ~46s.

Pending: manual DO console inspection, SSH connection test, destroy cycle,
clean redeploy (items 3–7).

## Live destroy (2026-05-12)

`scripts/destroy.sh` (interactive, answered `yes`). Full output in
`tests/results/PR-003-destroy.txt`.

- 7 resources destroyed, 0 remaining. No orphaned resources.
- `~/.ssh/config.d/sask` removed first (0s), before any cloud resource.
- DNS record removed immediately (0s) — no lingering DNS after destroy.
- Reserved IP released (0s) once IP assignment was detached — no dangling cost.
- Destroy timing: IP assignment detach 12s → droplet destroy 21s (sequential);
  everything else near-instant. Total ~33s.
- Droplet's ephemeral direct IP was `46.101.229.123` (different from reserved IP
  `206.189.249.29`) — confirms reserved IP design: direct IP changes on redeploy,
  reserved IP and DNS remain stable.

Pending: clean redeploy (item 6).
