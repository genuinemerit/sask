---
# Inventory for the sask droplet.
# The alias sask-droplet is defined in ~/.ssh/config.d/sask (generated by
# tofu apply), which maps it to the reserved IP.  We do NOT hardcode the IP
# here so that destroy/re-provision cycles require no inventory edits.
all:
  hosts:
    sask-droplet:
      ansible_user: root
