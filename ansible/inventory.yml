---
# Host referenced by its SSH alias only, never by IP — the alias (written by
# infra/tofu/ssh-config.tf into ~/.ssh/config.d/sask) is the single place an
# IP is ever resolved, so it stays valid across destroy/recreate cycles.
all:
  hosts:
    sask-droplet: {}
